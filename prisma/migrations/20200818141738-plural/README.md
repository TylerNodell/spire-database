# Migration `20200818141738-plural`

This migration has been generated by Tyler at 8/18/2020, 10:17:38 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200818140620-agent-owner-tenant-list..20200818141738-plural
--- datamodel.dml
+++ datamodel.dml
@@ -5,27 +5,27 @@
 ///
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model Owner {
   id        Int      @default(autoincrement()) @id
   createdAt DateTime @default(now())
   name      String
   phone     String?  @unique
   email     String?  @unique
-  Unit      Unit[]
+  units      Unit[]
 }
 model Tenant {
   id        Int      @default(autoincrement()) @id
   createdAt DateTime @default(now())
   name      String
   phone     String?  @unique
   email     String?  @unique
-  Unit      Unit?    @relation(fields: [unitId], references: [id])
+  unit      Unit?    @relation(fields: [unitId], references: [id])
   unitId    Int?
 }
 model Agent {
@@ -34,17 +34,17 @@
   name      String
   email     String   @unique
   password  String
   phone     String   @unique
-  Unit      Unit[]
+  units      Unit[]
 }
 model Building {
   id        Int      @default(autoincrement()) @id
   createdAt DateTime @default(now())
   address   String
   name      String?
-  Unit      Unit[]
+  units      Unit[]
 }
 model Unit {
   id         Int      @default(autoincrement()) @id
@@ -54,9 +54,9 @@
   tenantId   Int?     @unique
   apt        String
   sale       Boolean
   agentId    Int
-  Agent      Agent[]
-  Building   Building @relation(fields: [buildingId], references: [id])
-  Owner      Owner[]
-  Tenant     Tenant[]
+  agent      Agent[]
+  building   Building @relation(fields: [buildingId], references: [id])
+  owner      Owner[]
+  tenant     Tenant[]
 }
```


